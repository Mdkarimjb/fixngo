{
  "name": "fixngo",
  "private": true,
  "version": "0.1.0",
  "description": "FixNGo / Homi Services — unified PWA (Customer, Technician, Admin) for India",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace apps/api run start:dev",
    "dev:web": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/api run build && npm --workspace apps/web run build",
    "lint": "npm --workspace apps/api run lint && npm --workspace apps/web run lint",
    "quality": "node .github/skills/fixngo-tech-stack/scripts/quality-check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
